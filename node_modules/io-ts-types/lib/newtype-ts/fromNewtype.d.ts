import { AnyNewtype, CarrierOf } from 'newtype-ts';
import { Type, mixed } from 'io-ts';
export declare const fromNewtype: <N extends AnyNewtype = never>(codec: Type<CarrierOf<N>, CarrierOf<N>, mixed>) => Type<N, CarrierOf<N>, mixed>;
export declare const fromNewtypeCurried: <N extends AnyNewtype = never>() => <O>(codec: Type<CarrierOf<N>, O, mixed>) => Type<N, O, mixed>;
