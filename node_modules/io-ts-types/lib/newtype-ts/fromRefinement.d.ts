import { AnyNewtype, CarrierOf } from 'newtype-ts';
import { Prism } from 'monocle-ts';
import { Type, mixed } from 'io-ts';
export declare const fromRefinement: <S extends AnyNewtype = never>() => <O>(carrier: Type<CarrierOf<S>, O, mixed>, prism: Prism<CarrierOf<S>, S>, name?: string) => Type<S, O, mixed>;
